.DS_Store
.state/
.code-review-graph/

# Built by `node scripts/setup.mjs` from bin/swyh-rs-cli; the ad-hoc signature that carries
# the microphone grant is only valid on the machine that built it.
bin/MossCastHelper.app/

# Optional local settings (MOSS_NODE, ...).
moss.env
